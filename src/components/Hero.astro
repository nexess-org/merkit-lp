---
interface Props {
  ctaUrl: string;
}

const { ctaUrl } = Astro.props;
---

<section class="min-h-screen pt-24 pb-16 md:pt-32 md:pb-24 bg-gradient-mesh relative overflow-hidden">
  <!-- Subtle dot pattern -->
  <div class="absolute inset-0 bg-dots opacity-40"></div>

  <div class="max-w-6xl mx-auto px-6 relative z-10">
    <div class="grid lg:grid-cols-2 gap-12 lg:gap-16 items-center">
      <!-- Left: Content -->
      <div class="max-w-xl">
        <!-- Badge -->
        <div class="animate-fade-up">
          <span class="badge badge-accent">
            Chrome拡張機能
          </span>
        </div>

        <!-- Headline -->
        <h1 class="mt-6 text-4xl md:text-5xl lg:text-6xl font-display font-bold text-ink-900 leading-[1.1] tracking-tight animate-fade-up delay-100">
          メルカリの<br />
          面倒な作業を、<br />
          <span class="text-accent-500">一括で。</span>
        </h1>

        <!-- Subhead -->
        <p class="mt-6 text-lg text-ink-600 leading-relaxed animate-fade-up delay-200">
          価格変更もコメント投稿も、まとめて自動化。<br />
          何時間もかかる作業を、数分で完了させましょう。
        </p>

        <!-- CTAs -->
        <div class="mt-8 flex flex-col sm:flex-row gap-4 animate-fade-up delay-300">
          <a
            href={ctaUrl}
            class="inline-flex items-center justify-center gap-2 px-7 py-4 btn-accent text-base group"
          >
            <span>無料で始める</span>
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-4 h-4 group-hover:translate-x-1 transition-transform">
              <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3" />
            </svg>
          </a>
          <a
            href="#demo"
            class="inline-flex items-center justify-center gap-2 px-6 py-4 btn-outline text-base"
          >
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-5 h-5 text-accent-500">
              <path fill-rule="evenodd" d="M4.5 5.653c0-1.427 1.529-2.33 2.779-1.643l11.54 6.347c1.295.712 1.295 2.573 0 3.286L7.28 19.99c-1.25.687-2.779-.217-2.779-1.643V5.653Z" clip-rule="evenodd" />
            </svg>
            <span>デモを見る</span>
          </a>
        </div>

        <!-- Trial info -->
        <p class="mt-6 text-sm text-ink-500 animate-fade-up delay-400">
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" class="w-4 h-4 inline mr-1 text-green-600">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.857-9.809a.75.75 0 00-1.214-.882l-3.483 4.79-1.88-1.88a.75.75 0 10-1.06 1.061l2.5 2.5a.75.75 0 001.137-.089l4-5.5z" clip-rule="evenodd" />
          </svg>
          1週間無料トライアル・クレジットカード登録後すぐ利用可能
        </p>
      </div>

      <!-- Right: Screenshot Mockup -->
      <div class="relative animate-fade-up delay-500">
        <!-- Decorative elements -->
        <div class="absolute -top-8 -right-8 w-32 h-32 bg-accent-100 rounded-full blur-3xl"></div>
        <div class="absolute -bottom-8 -left-8 w-40 h-40 bg-cream-300 rounded-full blur-3xl"></div>

        <!-- Screenshot card -->
        <div class="relative bg-white rounded-2xl p-3 shadow-2xl shadow-ink-900/10 border border-cream-200 animate-float">
          <!-- Browser bar -->
          <div class="flex items-center gap-2 px-3 py-2 bg-cream-100 rounded-t-lg mb-2">
            <div class="flex gap-1.5">
              <div class="w-2.5 h-2.5 rounded-full bg-cream-400"></div>
              <div class="w-2.5 h-2.5 rounded-full bg-cream-400"></div>
              <div class="w-2.5 h-2.5 rounded-full bg-cream-400"></div>
            </div>
            <div class="flex-1 h-5 bg-cream-200 rounded mx-8"></div>
          </div>

          <!-- CSS Mockup UI -->
          <div class="bg-cream-50 rounded-lg p-4 aspect-[4/3]">
            <!-- Mockup Header -->
            <div class="flex items-center justify-between mb-4">
              <div class="flex items-center gap-2">
                <img src="/favicon.png" alt="Merkit" class="w-8 h-8 rounded-lg" />
                <span class="font-display font-semibold text-ink-900">Merkit</span>
              </div>
              <div class="px-3 py-1 bg-green-100 text-green-700 text-xs font-medium rounded-full">Premium</div>
            </div>

            <!-- Mockup Tabs -->
            <div class="flex gap-1 mb-4">
              <div class="px-3 py-1.5 bg-accent-500 text-white text-xs font-medium rounded-lg">価格変更</div>
              <div class="px-3 py-1.5 bg-cream-200 text-ink-500 text-xs rounded-lg">コメント</div>
              <div class="px-3 py-1.5 bg-cream-200 text-ink-500 text-xs rounded-lg">テンプレート</div>
            </div>

            <!-- Mockup Form -->
            <div class="space-y-3">
              <div class="flex items-center gap-3">
                <div class="flex items-center gap-2">
                  <div class="w-4 h-4 border-2 border-accent-500 rounded bg-accent-500 flex items-center justify-center">
                    <svg class="w-2.5 h-2.5 text-white" fill="currentColor" viewBox="0 0 20 20">
                      <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                    </svg>
                  </div>
                  <span class="text-xs text-ink-600">値下げ</span>
                </div>
                <div class="flex items-center gap-2">
                  <div class="w-4 h-4 border-2 border-cream-300 rounded"></div>
                  <span class="text-xs text-ink-400">値上げ</span>
                </div>
              </div>

              <div class="bg-white rounded-lg p-3 border border-cream-200">
                <div class="text-xs text-ink-400 mb-1">変更金額</div>
                <div class="flex items-baseline gap-1">
                  <span class="text-2xl font-display font-bold text-ink-900">100</span>
                  <span class="text-ink-500">円</span>
                </div>
              </div>

              <div class="bg-white rounded-lg p-3 border border-cream-200">
                <div class="text-xs text-ink-400 mb-1">最低価格</div>
                <div class="flex items-baseline gap-1">
                  <span class="text-lg font-display font-semibold text-ink-700">300</span>
                  <span class="text-ink-500 text-sm">円</span>
                </div>
              </div>

              <div class="bg-accent-500 text-white text-center py-2.5 rounded-lg text-sm font-medium">
                実行する
              </div>
            </div>
          </div>
        </div>

        <!-- Floating stat card -->
        <div class="absolute -bottom-4 -left-4 md:-left-8 bg-white rounded-xl p-4 shadow-xl shadow-ink-900/10 border border-cream-200 animate-fade-up delay-700">
          <div class="flex items-center gap-3">
            <div class="w-10 h-10 bg-green-100 rounded-lg flex items-center justify-center">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" class="w-5 h-5 text-green-600">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.857-9.809a.75.75 0 00-1.214-.882l-3.483 4.79-1.88-1.88a.75.75 0 10-1.06 1.061l2.5 2.5a.75.75 0 001.137-.089l4-5.5z" clip-rule="evenodd" />
              </svg>
            </div>
            <div>
              <p class="text-xs text-ink-500">処理完了</p>
              <p class="font-display font-bold text-ink-900">128商品</p>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Scroll indicator -->
    <div class="absolute bottom-8 left-1/2 -translate-x-1/2 animate-bounce hidden lg:block">
      <div class="w-6 h-10 border-2 border-cream-400 rounded-full p-1">
        <div class="w-1.5 h-2.5 bg-cream-400 rounded-full mx-auto animate-fade-in"></div>
      </div>
    </div>
  </div>
</section>
