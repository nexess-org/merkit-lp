---
// TODO: Replace with your actual Google Form URL
// Example: "https://docs.google.com/forms/d/e/YOUR_FORM_ID/viewform?embedded=true"
const googleFormUrl = "";
---

<section id="contact" class="py-24 md:py-32 section-darker relative overflow-hidden">
  <!-- Background pattern -->
  <div class="absolute inset-0 pattern-grid opacity-30"></div>

  <!-- Decorative orbs -->
  <div class="orb orb-red w-72 h-72 top-20 -left-36 opacity-20"></div>
  <div class="orb orb-white w-64 h-64 bottom-20 -right-32 opacity-15"></div>

  <div class="max-w-4xl mx-auto px-6 relative z-10">
    <!-- Section header -->
    <div class="text-center mb-12 scroll-animate">
      <span class="badge badge-primary mb-4">Contact</span>
      <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold text-white mb-4 section-title">
        お問い合わせ
      </h2>
      <p class="text-lg text-dark-200 section-subtitle">
        ご質問・ご要望など、お気軽にお問い合わせください
      </p>
    </div>

    <!-- Contact options -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-12">
      <div class="glass-card rounded-2xl p-6 card-hover scroll-animate" style="animation-delay: 0.1s;">
        <div class="w-12 h-12 rounded-xl icon-container flex items-center justify-center mb-4">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6 text-primary-400">
            <path stroke-linecap="round" stroke-linejoin="round" d="M9.879 7.519c1.171-1.025 3.071-1.025 4.242 0 1.172 1.025 1.172 2.687 0 3.712-.203.179-.43.326-.67.442-.745.361-1.45.999-1.45 1.827v.75M21 12a9 9 0 11-18 0 9 9 0 0118 0zm-9 5.25h.008v.008H12v-.008z" />
          </svg>
        </div>
        <h3 class="text-white font-semibold text-lg mb-2">一般的なご質問</h3>
        <p class="text-dark-300 text-sm leading-relaxed">
          機能や使い方について、お気軽にお問い合わせください。
        </p>
      </div>

      <div class="glass-card rounded-2xl p-6 card-hover scroll-animate" style="animation-delay: 0.2s;">
        <div class="w-12 h-12 rounded-xl icon-container flex items-center justify-center mb-4">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6 text-primary-400">
            <path stroke-linecap="round" stroke-linejoin="round" d="M11.42 15.17L17.25 21A2.652 2.652 0 0021 17.25l-5.877-5.877M11.42 15.17l2.496-3.03c.317-.384.74-.626 1.208-.766M11.42 15.17l-4.655 5.653a2.548 2.548 0 11-3.586-3.586l6.837-5.63m5.108-.233c.55-.164 1.163-.188 1.743-.14a4.5 4.5 0 004.486-6.336l-3.276 3.277a3.004 3.004 0 01-2.25-2.25l3.276-3.276a4.5 4.5 0 00-6.336 4.486c.091 1.076-.071 2.264-.904 2.95l-.102.085m-1.745 1.437L5.909 7.5H4.5L2.25 3.75l1.5-1.5L7.5 4.5v1.409l4.26 4.26m-1.745 1.437l1.745-1.437m6.615 8.206L15.75 15.75M4.867 19.125h.008v.008h-.008v-.008z" />
          </svg>
        </div>
        <h3 class="text-white font-semibold text-lg mb-2">技術的なサポート</h3>
        <p class="text-dark-300 text-sm leading-relaxed">
          エラーや不具合が発生した場合は、状況を詳しくお知らせください。
        </p>
      </div>
    </div>

    <!-- Google Form embed or placeholder -->
    <div class="scroll-animate" style="animation-delay: 0.3s;">
      {googleFormUrl ? (
        <div class="glass-card rounded-2xl overflow-hidden">
          <iframe
            src={googleFormUrl}
            width="100%"
            height="800"
            frameborder="0"
            marginheight="0"
            marginwidth="0"
            title="お問い合わせフォーム"
            class="w-full"
          >
            読み込んでいます...
          </iframe>
        </div>
      ) : (
        <!-- Placeholder form when Google Form URL is not set -->
        <div class="glass-card rounded-2xl p-8 md:p-10">
          <form class="space-y-6">
            <div>
              <label for="name" class="block text-white text-sm font-medium mb-2">お名前</label>
              <input
                type="text"
                id="name"
                name="name"
                class="w-full px-4 py-3 rounded-xl input-dark"
                placeholder="山田 太郎"
              />
            </div>

            <div>
              <label for="email" class="block text-white text-sm font-medium mb-2">メールアドレス</label>
              <input
                type="email"
                id="email"
                name="email"
                class="w-full px-4 py-3 rounded-xl input-dark"
                placeholder="example@email.com"
              />
            </div>

            <div>
              <label for="category" class="block text-white text-sm font-medium mb-2">お問い合わせ種別</label>
              <select
                id="category"
                name="category"
                class="w-full px-4 py-3 rounded-xl input-dark"
              >
                <option value="">選択してください</option>
                <option value="general">一般的なご質問</option>
                <option value="support">技術的なサポート</option>
                <option value="billing">お支払いについて</option>
                <option value="feature">機能リクエスト</option>
                <option value="other">その他</option>
              </select>
            </div>

            <div>
              <label for="message" class="block text-white text-sm font-medium mb-2">お問い合わせ内容</label>
              <textarea
                id="message"
                name="message"
                rows="5"
                class="w-full px-4 py-3 rounded-xl input-dark resize-none"
                placeholder="お問い合わせ内容をご記入ください..."
              ></textarea>
            </div>

            <div class="pt-4">
              <button
                type="submit"
                class="w-full py-4 px-6 text-center text-white font-semibold rounded-xl btn-primary glow-red-hover transition-all text-lg"
              >
                送信する
              </button>
            </div>

            <p class="text-dark-400 text-sm text-center">
              通常1〜2営業日以内にご返信いたします
            </p>
          </form>
        </div>
      )}
    </div>
  </div>
</section>

<script>
  // Scroll animation observer
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
        }
      });
    },
    { threshold: 0.1 }
  );

  document.querySelectorAll('#contact .scroll-animate').forEach((el) => {
    observer.observe(el);
  });
</script>
